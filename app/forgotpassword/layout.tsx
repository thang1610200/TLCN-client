import React from 'react'


export default function layoutForgotPassword({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>
            <body>{children}</body>
        </>
    )
}
